# Prompt — build feature

Implement F-xxx / page ___ in `complyin30` as a strangler change: freeze public URLs; add tests for authz/redaction; new Supabase changes as new migrations only. Report PR link.
